---
paths:
  - "src/**/*.py"
  - "tests/**/*.py"
---

# Python

- Type hints completos.
- Pydantic v2 en todos los límites.
- `pathlib.Path`.
- Excepciones de dominio explícitas.
- Configuración con Pydantic Settings.
- JSON estable: UTF-8, claves ordenadas y formato legible.
- No usar estado global mutable.
- API y CLI llaman a servicios de aplicación comunes.
- El pipeline no depende de FastAPI.
- `subprocess` sin shell para invocar Java.
- `httpx.AsyncClient` para LLM.
- El contenido COBOL, CSV, XML y JSON se trata como dato no confiable.
